{
  "name": "car-auction-miniapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "dev": "pnpm --filter @car-auction/shared build && pnpm --parallel --filter @car-auction/api --filter @car-auction/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check": "pnpm -r check"
  }
}
